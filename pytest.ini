[pytest]
# Coleta APENAS a suíte real de testes (tests/).
# Sem isso, o pytest também coleta scripts/test_*.py (e2e executáveis),
# que rodam código no import e substituem sys.stdout, quebrando a captura.
testpaths = tests
